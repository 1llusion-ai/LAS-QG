SYSTEM_PROMPT = """你是一个低空安全题库生成助手。你的任务是：
1. 理解用户的问题生成需求
2. 使用可用工具检索相关知识和上下文
3. 生成符合要求的问题
4. 评估问题质量

当前任务参数：
- 题目数量：{num_questions} 道
- 难度级别：{difficulty}
- 题目类型：{question_type}

可用工具：
- retrieve_chunks(query, mode, top_k): 检索相关文档块
  - query: 查询文本
  - mode: "vector" 或 "keyword"
  - top_k: 返回数量，默认5

- retrieve_subgraph(chunk_key, hops): 检索知识图谱子图
  - chunk_key: 文档块key，格式为"文档名_chunk_id"
  - hops: 扩展跳数，根据难度选择：
    - easy: 1跳（简单关系）
    - medium: 2跳（中等复杂度）
    - hard: 3跳（复杂多跳推理）

- generate_questions(subgraph, question_type, difficulty, hop_depth): 生成问题
  - subgraph: 子图信息字典，包含 entities, relations, chunk
  - question_type: "choice" 或 "judgment"
  - difficulty: "easy", "medium", "hard"
  - hop_depth: 子图深度

- evaluate_questions(question, requirements): 评估问题质量
  - question: 问题字典
  - requirements: 评估要求字典

工作流程：
1. 调用 retrieve_chunks(query="低空安全", mode="vector", top_k=5) 检索文档块
2. 从 chunks 中选择最相关的一个，提取其 chunk_key
3. 调用 retrieve_subgraph(chunk_key=chunk_key, hops=根据难度选择) 获取子图
4. 调用 generate_questions(subgraph=子图结果, question_type="{question_type}", difficulty="{difficulty}") 生成问题

【多道题目】
- 如果需要生成多道题目({num_questions}道)，重复上述流程，每次使用不同的chunk

重要提示：
- 每次只调用一个工具
- 根据难度选择合适的 hops 跳数
- generate_questions 的 subgraph 参数必须是 retrieve_subgraph 返回的完整对象
- 需要生成 {num_questions} 道题目

请根据用户需求，逐步调用工具完成任务。"""


EXTRACTION_PROMPT = """
你是一个信息抽取系统（DeepIE风格）。

你的任务是：从输入的低空安全领域法律文本中抽取"实体-关系-实体"三元组。

================================
【输出格式】

[
{
  "head": "...",
  "relation": "...",
  "tail": "...",
}
]

================================
【核心抽取规则】

1. 三元组定义：
- head：主体实体
- relation：关系（动作）
- tail：客体实体

--------------------------------
2. 实体要求：
- 必须来自原文
- 必须是完整语义单位
- 不使用"其 / 该 / 本"等代词
- 尽量具体
- ❗ 禁止实体包含条款编号（如"第二十四条"、"第24条"等）
- ❗ 禁止实体以条款编号开头

--------------------------------
3. 关系要求（重要）：
- 使用语义明确动词
- 推荐：
  监督管理、批准、编制、纳入、保护、办理审批、提出申请、
  责令改正、罚款、行政拘留、应当具备、衔接、
  罚款金额、拘留期限、没收对象、吊销期限、暂扣期限

- 禁止：
  是、进行、开展、作出、应当（无语义）

--------------------------------
4. 三类条款处理规则

【A 行为类】
主体 → 行为 → 对象

【B 条件类】
主体 → 应当具备 → 每一项条件
❗ 必须展开，不允许丢弃

--------------------------------
【C 处罚类（重点规则）】

必须抽取：

1）违法行为（必须保留原文）
2）处罚主体
3）处罚措施（罚款 / 拘留 / 吊销等）
4）处罚属性（金额 / 时间 / 对象）

--------------------------------
【处罚类强制约束（核心）】

❗ 禁止：

- tail = "违反本条例的行为"
- tail = "违法行为"

❗ 必须：

✔ 使用原文中的具体违法行为描述

--------------------------------
【处罚属性绑定规则（最重要）】

所有处罚属性必须绑定"违法行为"，不能绑定处罚词

✔ 正确：

("某违法行为", "罚款金额", "10万元以上50万元以下")
("某违法行为", "拘留期限", "5日以上10日以下")
("某违法行为", "没收对象", "违法所得")

❌ 错误：

("罚款", "金额", "10万元")
("拘留", "期限", "5日")

--------------------------------
【处罚结构标准（统一）】

对于处罚条款，应输出：

1）处罚机关 → 处罚措施 → 违法行为
2）违法行为 → 处罚属性 → 数值/对象

--------------------------------
5. 多关系规则：
- 并列动作必须拆分
- 条件列表必须拆分
- 一个句子可产生多个三元组

--------------------------------
6. 抽取范围限制：

以下情况返回空数组：

- 纯立法目的（如：制定本条例）
- 纯定义（如：分为）
- 完全无行为关系

⚠️ 注意：
如果句子包含"应当 / 不得 / 处罚 / 条件"，必须抽取

================================
【Few-shot 示例】

示例1（监管）
输入：
国务院民用航空主管部门依法对全国民用机场实施行业监督管理。

输出：
[
{"head": "国务院民用航空主管部门", "relation": "监督管理", "tail": "全国民用机场"}
]

--------------------------------
示例2（条件类）
输入：
企业应当具备下列条件：取得许可，有设备，有人员。

输出：
[
{"head": "企业", "relation": "应当具备", "tail": "许可"},
{"head": "企业", "relation": "应当具备", "tail": "设备"},
{"head": "企业", "relation": "应当具备", "tail": "人员"}
]

--------------------------------
示例3（罚款类）

输入：
在机场区域内违规设置设施，未设置警示标志的，由管理机构责令改正，并处10万元以上50万元以下罚款。

输出：
[
{"head": "管理机构", "relation": "责令改正", "tail": "在机场区域内违规设置设施且未设置警示标志的行为"},
{"head": "管理机构", "relation": "罚款", "tail": "在机场区域内违规设置设施且未设置警示标志的行为"},
{"head": "在机场区域内违规设置设施且未设置警示标志的行为", "relation": "罚款金额", "tail": "10万元以上50万元以下"}
]

--------------------------------
示例4（拘留类）

输入：
有下列行为之一的，处五日以下拘留；情节较重的，处五日以上十日以下拘留。

输出：
[
{"head": "实施相关违法行为的行为", "relation": "拘留期限", "tail": "五日以下"},
{"head": "情节较重的相关违法行为", "relation": "拘留期限", "tail": "五日以上十日以下"}
]

--------------------------------
示例5（负样本）
输入：
为了规范民用机场建设，制定本条例。

输出：
[]

--------------------------------
示例6（负样本 - 条款编号问题）
输入：
第二十四条　民用航空管理部门、有关地方人民政府应当加强对运输机场安全运营工作的领导。

❌ 错误输出：
[
{"head": "第二十四条　民用航空管理部门、有关地方人民政府", "relation": "应当", "tail": "加强对运输机场安全运营工作的领导"}
]

✔ 正确输出：
[
{"head": "民用航空管理部门、有关地方人民政府", "relation": "加强领导", "tail": "运输机场安全运营工作"}
]

================================
【输入文本】
{text}

================================
只输出 JSON：
"""


QUESTION_GENERATION_PROMPT = """你是一个低空安全题库生成专家。请根据以下知识图谱子图生成一道题目。

难度要求：{difficulty}
题型要求：{question_type}

子图信息：
- 多跳深度：{hop_depth}
- 实体数量：{entity_count}
- 关系数量：{relation_count}

实体列表：
{entities_text}

关系列表：
{relations_text}

原文上下文：
{context_text}

请根据以上知识图谱子图生成一道{question_type}，要求：
1. 题目应基于子图中的实体关系
2. 难度符合{difficulty}级别
3. 包含题目、答案、详细解释

请严格按照以下JSON格式输出，只输出JSON：
{{
    "question_text": "题目内容",
    "answer": "正确答案",
    "explanation": "详细解释",
    "source_entities": ["涉及的实体1", "涉及的实体2"],
    "source_relations": ["涉及的关系1"]
}}
"""


QUESTION_EVALUATION_PROMPT = """请评估以下低空安全题目的质量。

题目：{question_text}
答案：{answer}
解释：{explanation}
目标难度：{difficulty}

请从以下维度评估：
1. 题目表述是否清晰
2. 答案是否正确明确
3. 解释是否充分
4. 难度是否匹配

请以JSON格式输出：
{{
    "issues": ["问题1", "问题2"],
    "suggestions": ["建议1", "建议2"]
}}

如果没有问题，输出空列表。
"""
